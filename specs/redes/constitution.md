# Constitution

- Mantener `Redes` como asignatura de segundo curso, segundo cuatrimestre.
- Separar el acceso por modos: `autoevaluación`, `cuestionarios de temario` y `conceptos importantes`.
- Reutilizar el flujo visual y de persistencia de `quiz` siempre que no rompa a las asignaturas existentes.
- Resolver y mostrar contenido exclusivamente a partir del material local de `temp/REDES` y de los datos generados en `public/data/quiz/redes`.
